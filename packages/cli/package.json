{
  "name": "modu",
  "version": "0.1.0",
  "description": "CLI to monetize any API with x402 on Algorand",
  "type": "module",
  "bin": {
    "modu": "./bin/modu.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "tsx --test tests/**/*.test.ts",
    "clean": "node -e \"try { require('fs').rmSync('dist', { recursive: true, force: true }) } catch (e) {}\""
  },
  "dependencies": {
    "@modu/shared": "*",
    "algosdk": "^2.7.0",
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.5",
    "commander": "^12.0.0",
    "open": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "tsx": "^4.10.5",
    "typescript": "^5.4.5"
  }
}
